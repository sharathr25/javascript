var studentName
console.log(typeof studentName) // "undefined"

console.log(typeof doesntExist) // "undefined"

// The above code is confusing
// because we are getting undefined for both declared variable and non-declare variable.
// But JS is treating both of this in a same manner
